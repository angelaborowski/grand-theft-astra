{
  "name": "@gpta/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --assets ../web/public --port 8787",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@gpta/core": "workspace:*",
    "cookie": "catalog:",
    "openai": "catalog:",
    "ws": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "wrangler": "catalog:"
  }
}
